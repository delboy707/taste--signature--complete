# Backup files from patch scripts
*.bak
*.bak.*

# Patch scripts (local tooling only, not repo content)
prompt-*-patch*.js
remove-legacy-*.js
*-patch.js
*-patch-v*.js

# macOS system files
.DS_Store
.AppleDouble
.LSOverride

# Editor temp files
*.swp
*.swo
*~
.vscode/
.idea/

# Node
node_modules/
npm-debug.log*

# Environment / secrets
.env
.env.local
.env.*.local

# Firebase emulator artifacts (test/emulator.test.js - never a real project)
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# Service account keys - never committed, regardless of where they land
# (scripts/export-experiences-backup.js also refuses to run against one
# found inside the repo, as a second layer of defence)
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json

# Output of scripts/export-experiences-backup.js - real production data
backups/
